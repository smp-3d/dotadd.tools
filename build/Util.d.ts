export declare class ParseError {
    constructor(filename: string, message: string);
    message: string;
    file: string;
}
export declare class ExportError {
    constructor(filename: string, message: string);
    message: string;
    file: string;
}
